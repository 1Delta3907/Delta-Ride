/*function calculateRotations(data){
	rotations=data.length*10*20;
	//10 is the number of rotations per data element and the string of data is 20

	return rotations;
}*/
//var csv= require("fast-csv");
//var counts = {};
//rows.forEach(function(r) {
  //  var key = r.Day + r.What;
    //if (!counts[key]) {
      //  counts[key] = {
        //   day: r.Day,
          // what: r.What,
//           count: 0
//        };
  //  }
   // counts[key].count++;
//});

//var data = [];
//Object.keys(counts).forEach(function(key) {
//    data.push(counts[key]);
